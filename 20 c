#include<iostream>
#include<cmath>
using namespace std;
int main(){
float x,y;
cin >> x;
cin >> y;
if((y<=2-x*x) and (y>=x) or (y<=2-x*x) and (y<=x) and (y>=0) and (x>=0)){
cout << "YES";
}
else{
cout << "NO";
}
}
