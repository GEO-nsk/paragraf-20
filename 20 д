#include<iostream>
#include<cmath>
using namespace std;
int main(){
float x,y;
cin >> x;
cin >> y;
if((x*x + y*y <=1) and (y>=x) or (x*x + y*y <=1) and (y<=x) and (x<=0)){
cout << "YES";
}
else{
cout << "NO";
}
}
